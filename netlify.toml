[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: todas las rutas del cliente sirven index.html (equivalente al rewrite de vercel.json).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
